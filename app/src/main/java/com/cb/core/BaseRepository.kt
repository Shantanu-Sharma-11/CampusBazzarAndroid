package com.cb.core

/**
 * Marker interface for all repositories to ensure standard architecture layering.
 * Common data-layer error handling or retry logic can be defined here later.
 */
interface BaseRepository {
    // Empty for now, acts as a structural foundation.
}
